The editor package mainly handles packaging the application in an executable.

The main logic resides in `core` and the actual user interface is built in `ui`,
this package handles bootstrapping and packaging.