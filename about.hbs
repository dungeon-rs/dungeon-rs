<!DOCTYPE html>
<html lang="en">
<head>
    <title>DungeonRS - licenses</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
<main class="prose dark:prose-invert mx-auto px-4 py-8 max-w-4xl">
    <div class="text-center mb-8">
        <h1 class="text-3xl font-bold mb-2">Third Party Licenses</h1>
        <p class="text-gray-600 dark:text-gray-300">This page lists the licenses of the projects used in DungeonRS.</p>
    </div>

    <h2 class="text-2xl font-semibold mt-12 mb-4">Overview of licenses:</h2>
    <ul class="list-disc list-inside space-y-1">
        {{#each overview}}
            <li><a href="#{{id}}">{{name}}</a> ({{count}})</li>
        {{/each}}
    </ul>

    <h2 class="text-2xl font-semibold mt-12 mb-4">All license text:</h2>
    <ul class="space-y-8">
        {{#each licenses}}
            <li class="license">
                <h3 id="{{id}}" class="text-xl font-semibold text-blue-700 dark:text-blue-300">{{name}}</h3>
                <h4 class="text-lg font-medium text-gray-700 dark:text-gray-200 mt-4">Used by:</h4>
                <ul class="list-disc list-inside ml-4">
                    {{#each used_by}}
                        <li><a href="{{#if
                                crate.repository}} {{crate.repository}} {{else}} https://crates.io/crates/{{crate.name}} {{/if}}">{{crate.name}} {{crate.version}}</a>
                        </li>
                    {{/each}}
                </ul>
                <pre class="bg-gray-100 dark:bg-gray-800 p-4 rounded overflow-y-auto max-h-96 whitespace-pre-wrap text-sm text-gray-800 dark:text-gray-100">{{text}}</pre>
            </li>
        {{/each}}
    </ul>
</main>
</body>
</html>
